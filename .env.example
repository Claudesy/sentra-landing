# Blueprinted & built by Claudesy.
# Environment Variables — copy this file to .env.local for local overrides
#
# This project currently requires NO environment variables for local development.
# All content is hardcoded; no external APIs are called at runtime.
#
# If you add runtime integrations (analytics, CMS, AI APIs), document them here:

# Example (not currently used):
# NEXT_PUBLIC_ANALYTICS_ID=your-analytics-id
# NEXT_PUBLIC_API_BASE_URL=https://api.sentra.ai
